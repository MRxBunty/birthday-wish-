function wishHappyBirthday(name: string): string {
    return `Happy Birthday, ${name}! 🎉🎂`;
}

const birthdayPerson = "RAHEEL";
console.log(wishHappyBirthday(birthdayPerson));

let age = "19"
console.log(`Congratulation MR RAHEEL Now You Are `+(age)+`Year Old `);
